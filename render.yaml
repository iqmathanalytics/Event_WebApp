# Render Blueprint — set secrets (DB_*, JWT_*, etc.) in the Render dashboard or here as sync: false.
services:
  - type: web
    name: yay-tickets-api
    runtime: node
    region: oregon
    plan: free
    rootDir: .
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
