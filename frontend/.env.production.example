# Copy to frontend/.env BEFORE npm run build (values are baked into the static build).

# MilesWeb: API subdomain + /api proxy path (adjust to your host)
VITE_API_BASE_URL=https://www.bookmytickets.us/api

VITE_GOOGLE_CLIENT_ID=
VITE_STRIPE_PUBLISHABLE_KEY=
VITE_GA_MEASUREMENT_ID=
