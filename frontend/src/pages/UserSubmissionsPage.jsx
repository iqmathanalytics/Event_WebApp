import UserSubmissionsPanel from "../components/UserSubmissionsPanel";

/** Standalone route; same panel is embedded on the user hub dashboard. */
export default function UserSubmissionsPage() {
  return <UserSubmissionsPanel variant="standalone" />;
}
