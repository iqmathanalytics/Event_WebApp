/** Duration/ease for measured splash → header logo flight (`LogoFlightOverlay`). */
export const BRAND_LOGO_HANDOFF_DURATION = 1.05;
export const BRAND_LOGO_HANDOFF_EASE = [0.19, 1, 0.22, 1];

/** Overlay out + header logo in (seconds). */
export const BRAND_LOGO_CROSSFADE_DURATION = 0.24;
